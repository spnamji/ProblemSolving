# 2068. 최대수 구하기
T = int(input())

for test_case in range(1, T + 1):
    num = list(map(int, input().split()))
    max_num = max(num)  # max함수를 사용하여 최대값을 구함
    print('#{0} {1}'.format(test_case, max_num)) # format함수로 출력 구현
